export:
  gaussian_splat: true
  point_cloud: true

  # Extra args appended to ns-export commands
  splat_extra_args: []
  pointcloud_extra_args: []

  # If empty, exporter auto-detects the latest Nerfstudio run folder under outputs/
  run_dir: ""
