ffmpeg -version
ffprobe -version

colmap -h

py -3.10 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip


pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

pip install -r requirements.txt


ns-process-data --help
ns-train --help
ns-export --help


python -c "import torch; print('torch:', torch.__version__); print('cuda available:', torch.cuda.is_available()); print('device:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else None)"


# 1) Extract smart frames (creates experiments/<exp>/<ver>/frames)
python -m src.cli --exp dji0004 --ver v0001 extract --video dji0004.mp4

# 2) Create nerfstudio dataset (ns-process-data images)
python -m src.cli --exp dji0004 --ver v0001 process

# 3) Train splatfacto
python -m src.cli --exp dji0004 --ver v0001 train

# 4) Export gaussian splat
python -m src.cli --exp dji0004 --ver v0001 export

or

python -m src.cli --exp dji0004 --ver v0003 all --video dji0004.mp4

